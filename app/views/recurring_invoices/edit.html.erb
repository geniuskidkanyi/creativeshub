<div class="px-6 py-6 max-w-3xl">
  <div class="mb-6">
    <%= link_to "← Back", recurring_invoice_path(@recurring_invoice), class: "text-xs text-muted-foreground hover:text-foreground transition-colors" %>
    <h1 class="text-2xl text-foreground mt-3" style="font-family: var(--font-display)">Edit schedule</h1>
  </div>
  <%= render "form" %>
</div>
